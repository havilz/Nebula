# IOKit Console Driver Headers

Folder `include/iokit/console/` berisi deklarasi driver konsol teks VGA.

## Source Tree

```text
include/iokit/console/
└── console.hpp  # VGA Text Mode Console Driver Header
```

## Daftar File & Fungsi

| File | Deskripsi & Fungsi |
| :--- | :--- |
| `console.hpp` | Deklarasi fungsi penulisan karakter dan string ke layar VGA Text Mode 80x25 (`0xB8000`). |
